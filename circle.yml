machine:
  ruby:
    version: 2.2.0

## Customize dependencies
dependencies:
  pre:
    - gem install bundler

  override:
    - bundle install --path=.bundle -j4

## Customize test commands
test:
  override:
    - rake
